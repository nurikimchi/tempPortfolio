import { ArticleLayout } from '@/components/ArticleLayout'

export const meta = {
  author: 'Nuri Kim',
  date: '2024-03-02',
  title: 'How to Coordinate GLTF Animations With Rotation and Position',
    description: 
    'In A-Frame, developers can easily animate an entity\'s rotation and position. However, there are no built-in solutions for adding clip-animations. In this article, I propose a solution to coordinate a CAD model\'s clip animations with its A-Frame counterpart.'
}

export default (props) => <ArticleLayout meta={meta} {...props} />

## Adding position/rotation animations

## No built-in support for A-Frame animation with clips

## Coordinating built-in with clip animations

## File structure
